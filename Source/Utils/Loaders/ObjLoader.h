#pragma once

#include <iostream>
#include <vector>

#include "../../Graphics/Rendering/Renderable3D.h"
/*
namespace Derpy { namespace Utils {

	class ObjLoader{
	private:
		
	public:
		ObjLoader();
		ObjLoader(const char* path, Graphics::Renderable3D& renderable);
		~ObjLoader();

		void load(const char* path, Graphics::Renderable3D& renderable);
		void unload();

		Maths::Vec3f* parseVertex(const char* line);
		Maths::Vec3f* parseNormal(const char* line);

	private:
		
	};


} }
*/